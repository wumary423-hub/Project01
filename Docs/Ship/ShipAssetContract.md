# Ship Asset Contract — SUPERSEDED

**状态：SUPERSEDED。** 原 V0.1 合同正文已迁入统一规范，不再在此维护。

请改读：

- `Docs/specs/systems/ship-system/ship-system.md`
- `Docs/specs/assets/ships/ShipType_SmallSailer01/integration.yaml`

Socket、MastRig、路径以 `docs/specs` 为准。`.cursor/rules/ship-system.mdc` 仅为 Cursor 入口。
